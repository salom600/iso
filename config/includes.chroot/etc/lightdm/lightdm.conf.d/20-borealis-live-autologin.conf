[LightDM]
# Live-session autologin. Calamares' shellprocess removes this file on install,
# so the installed system always gets a normal login screen.
autologin-user=borealis
autologin-user-timeout=0
autologin-session=borealis
